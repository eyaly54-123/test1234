<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>EscapeIQ – פלטפורמת חדרי הבריחה החכמה</title>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;600;700;900&family=Heebo:wght@300;400;500;700&display=swap" rel="stylesheet"/>
  <style>
    :root {
      --cyan: #00d4ff;
      --magenta: #f059f0;
      --purple: #a855f7;
      --dark: #050810;
      --dark2: #080d1a;
      --dark3: #0c1428;
      --card: rgba(12,20,40,0.85);
      --border: rgba(0,212,255,0.18);
      --text: #e8edf5;
      --muted: #7889a8;
    }

    * { margin:0; padding:0; box-sizing:border-box; }

    html { scroll-behavior: smooth; }

    body {
      background: var(--dark);
      color: var(--text);
      font-family: 'Heebo', sans-serif;
      overflow-x: hidden;
    }

    /* ── GRID BG ── */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(rgba(0,212,255,0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,212,255,0.03) 1px, transparent 1px);
      background-size: 52px 52px;
      pointer-events: none;
      z-index: 0;
    }

    /* ── SCANLINES ── */
    body::after {
      content: '';
      position: fixed;
      inset: 0;
      background: repeating-linear-gradient(
        0deg,
        transparent,
        transparent 2px,
        rgba(0,0,0,0.08) 2px,
        rgba(0,0,0,0.08) 4px
      );
      pointer-events: none;
      z-index: 0;
    }

    /* ── NAV ── */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 18px 48px;
      background: rgba(5,8,16,0.8);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
    }

    .nav-logo {
      font-family: 'Orbitron', monospace;
      font-size: 22px;
      font-weight: 900;
      letter-spacing: 3px;
      text-decoration: none;
    }
    .nav-logo .c { color: var(--cyan); }
    .nav-logo .m { color: var(--magenta); }

    .nav-links {
      display: flex;
      gap: 32px;
      list-style: none;
    }
    .nav-links a {
      color: var(--muted);
      text-decoration: none;
      font-size: 14px;
      letter-spacing: 1px;
      transition: color .2s;
    }
    .nav-links a:hover { color: var(--cyan); }

    .nav-cta {
      display: flex;
      gap: 12px;
    }

    .btn {
      padding: 10px 24px;
      border-radius: 6px;
      font-family: 'Orbitron', monospace;
      font-size: 11px;
      font-weight: 700;
      letter-spacing: 1.5px;
      cursor: pointer;
      border: none;
      transition: all .25s;
      text-decoration: none;
    }
    .btn-ghost {
      background: transparent;
      border: 1px solid var(--border);
      color: var(--cyan);
    }
    .btn-ghost:hover {
      background: rgba(0,212,255,0.08);
      border-color: var(--cyan);
      box-shadow: 0 0 20px rgba(0,212,255,0.2);
    }
    .btn-primary {
      background: linear-gradient(135deg, var(--cyan), var(--purple));
      color: #fff;
      box-shadow: 0 4px 20px rgba(0,212,255,0.3);
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 30px rgba(0,212,255,0.4);
    }

    /* ── HERO ── */
    .hero {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 120px 24px 80px;
      overflow: hidden;
      z-index: 1;
    }

    /* Radial glow blobs */
    .hero-blob {
      position: absolute;
      border-radius: 50%;
      filter: blur(80px);
      pointer-events: none;
    }
    .blob1 {
      width: 500px; height: 500px;
      top: -100px; left: -150px;
      background: radial-gradient(circle, rgba(0,212,255,0.12), transparent 70%);
      animation: blobFloat 8s ease-in-out infinite;
    }
    .blob2 {
      width: 400px; height: 400px;
      top: 100px; right: -100px;
      background: radial-gradient(circle, rgba(168,85,247,0.14), transparent 70%);
      animation: blobFloat 10s ease-in-out infinite reverse;
    }
    .blob3 {
      width: 300px; height: 300px;
      bottom: 50px; left: 30%;
      background: radial-gradient(circle, rgba(240,89,240,0.10), transparent 70%);
      animation: blobFloat 12s ease-in-out infinite;
    }
    @keyframes blobFloat {
      0%,100% { transform: translateY(0) scale(1); }
      50% { transform: translateY(-30px) scale(1.05); }
    }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: rgba(0,212,255,0.08);
      border: 1px solid rgba(0,212,255,0.25);
      border-radius: 100px;
      padding: 6px 18px;
      font-size: 11px;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--cyan);
      margin-bottom: 28px;
      animation: fadeInDown .6s ease both;
    }
    .badge-dot {
      width: 6px; height: 6px;
      background: var(--cyan);
      border-radius: 50%;
      animation: pulse 2s infinite;
    }
    @keyframes pulse {
      0%,100% { opacity:1; box-shadow:0 0 0 0 rgba(0,212,255,0.5); }
      50% { opacity:.7; box-shadow:0 0 0 6px rgba(0,212,255,0); }
    }

    .hero-title {
      font-family: 'Orbitron', monospace;
      font-size: clamp(42px, 7vw, 88px);
      font-weight: 900;
      line-height: 1.0;
      letter-spacing: -1px;
      margin-bottom: 24px;
      animation: fadeInDown .7s .1s ease both;
    }
    .hero-title .line1 { display: block; color: #fff; }
    .hero-title .line2 {
      display: block;
      background: linear-gradient(135deg, var(--cyan) 0%, var(--magenta) 60%, var(--purple) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      filter: drop-shadow(0 0 30px rgba(0,212,255,0.4));
    }

    .hero-sub {
      font-size: 18px;
      font-weight: 300;
      color: var(--muted);
      max-width: 560px;
      margin: 0 auto 44px;
      line-height: 1.7;
      animation: fadeInDown .7s .2s ease both;
    }

    .hero-actions {
      display: flex;
      gap: 16px;
      justify-content: center;
      flex-wrap: wrap;
      animation: fadeInDown .7s .3s ease both;
    }

    .btn-hero {
      padding: 16px 40px;
      font-size: 13px;
    }

    .hero-stats {
      display: flex;
      gap: 48px;
      justify-content: center;
      margin-top: 72px;
      animation: fadeInUp .8s .5s ease both;
    }
    .hero-stat {
      text-align: center;
    }
    .stat-val {
      font-family: 'Orbitron', monospace;
      font-size: 28px;
      font-weight: 700;
      color: var(--cyan);
      display: block;
    }
    .stat-label {
      font-size: 11px;
      color: var(--muted);
      letter-spacing: 2px;
      text-transform: uppercase;
      margin-top: 4px;
    }

    /* floating rooms preview */
    .hero-cards-row {
      display: flex;
      gap: 20px;
      justify-content: center;
      margin-top: 60px;
      animation: fadeInUp .9s .6s ease both;
      flex-wrap: wrap;
    }
    .room-card {
      width: 200px;
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 20px 18px;
      text-align: right;
      transition: transform .3s, box-shadow .3s;
      backdrop-filter: blur(10px);
      cursor: pointer;
    }
    .room-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 20px 50px rgba(0,212,255,0.15);
      border-color: rgba(0,212,255,0.4);
    }
    .room-icon { font-size: 32px; margin-bottom: 10px; }
    .room-name {
      font-family: 'Orbitron', monospace;
      font-size: 11px;
      font-weight: 700;
      color: var(--cyan);
      letter-spacing: 1px;
      margin-bottom: 6px;
    }
    .room-desc { font-size: 12px; color: var(--muted); line-height: 1.5; }
    .room-tag {
      display: inline-block;
      margin-top: 10px;
      padding: 3px 10px;
      background: rgba(0,212,255,0.1);
      border: 1px solid rgba(0,212,255,0.2);
      border-radius: 4px;
      font-size: 10px;
      color: var(--cyan);
      letter-spacing: 1px;
    }

    /* ── SECTION BASE ── */
    section {
      position: relative;
      z-index: 1;
      padding: 100px 48px;
    }
    .section-tag {
      display: inline-block;
      font-size: 10px;
      letter-spacing: 4px;
      text-transform: uppercase;
      color: var(--cyan);
      background: rgba(0,212,255,0.08);
      border: 1px solid rgba(0,212,255,0.2);
      padding: 5px 14px;
      border-radius: 4px;
      margin-bottom: 18px;
    }
    .section-title {
      font-family: 'Orbitron', monospace;
      font-size: clamp(28px, 4vw, 48px);
      font-weight: 700;
      line-height: 1.15;
      margin-bottom: 20px;
    }
    .section-title span {
      background: linear-gradient(135deg, var(--cyan), var(--purple));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    .section-sub {
      font-size: 16px;
      color: var(--muted);
      line-height: 1.7;
      max-width: 540px;
    }

    /* ── FEATURES ── */
    .features {
      max-width: 1200px;
      margin: 0 auto;
    }
    .features-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 24px;
      margin-top: 56px;
    }
    .feat-card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 32px 28px;
      transition: all .3s;
      backdrop-filter: blur(12px);
    }
    .feat-card:hover {
      border-color: rgba(0,212,255,0.35);
      box-shadow: 0 0 40px rgba(0,212,255,0.08);
      transform: translateY(-4px);
    }
    .feat-icon {
      font-size: 36px;
      margin-bottom: 18px;
      display: block;
      filter: drop-shadow(0 0 10px rgba(0,212,255,0.4));
    }
    .feat-title {
      font-family: 'Orbitron', monospace;
      font-size: 14px;
      font-weight: 700;
      color: #fff;
      margin-bottom: 12px;
      letter-spacing: .5px;
    }
    .feat-desc { font-size: 14px; color: var(--muted); line-height: 1.7; }
    .feat-chip {
      display: inline-block;
      margin-top: 16px;
      padding: 4px 12px;
      border-radius: 4px;
      font-size: 10px;
      letter-spacing: 1.5px;
      font-weight: 700;
      text-transform: uppercase;
    }
    .chip-pro { background: rgba(168,85,247,0.15); color: var(--purple); border: 1px solid rgba(168,85,247,0.3); }
    .chip-ultra { background: rgba(240,89,240,0.12); color: var(--magenta); border: 1px solid rgba(240,89,240,0.3); }
    .chip-free { background: rgba(0,212,255,0.1); color: var(--cyan); border: 1px solid rgba(0,212,255,0.2); }

    /* ── MODES ── */
    .modes-section {
      background: linear-gradient(180deg, transparent, rgba(0,212,255,0.03), transparent);
    }
    .modes-inner {
      max-width: 1200px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 64px;
      align-items: center;
    }
    .modes-list {
      display: flex;
      flex-direction: column;
      gap: 16px;
      margin-top: 40px;
    }
    .mode-item {
      display: flex;
      gap: 16px;
      align-items: flex-start;
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 18px 20px;
      cursor: pointer;
      transition: all .25s;
    }
    .mode-item:hover, .mode-item.active {
      border-color: rgba(0,212,255,0.4);
      background: rgba(0,212,255,0.05);
      box-shadow: 0 0 25px rgba(0,212,255,0.08);
    }
    .mode-emoji { font-size: 24px; flex-shrink: 0; margin-top: 2px; }
    .mode-info h4 {
      font-family: 'Orbitron', monospace;
      font-size: 12px;
      color: #fff;
      margin-bottom: 5px;
      letter-spacing: .5px;
    }
    .mode-info p { font-size: 13px; color: var(--muted); line-height: 1.5; }

    /* visual display */
    .modes-display {
      position: relative;
    }
    .modes-screen {
      background: var(--dark2);
      border: 1px solid var(--border);
      border-radius: 20px;
      overflow: hidden;
      aspect-ratio: 4/3;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
    }
    .screen-glow {
      position: absolute;
      inset: 0;
      background: radial-gradient(ellipse at 50% 30%, rgba(0,212,255,0.08), transparent 60%);
    }
    .screen-content {
      text-align: center;
      position: relative;
      z-index: 1;
    }
    .screen-icon { font-size: 64px; display: block; margin-bottom: 16px; }
    .screen-label {
      font-family: 'Orbitron', monospace;
      font-size: 18px;
      font-weight: 700;
      color: var(--cyan);
    }
    /* animated bars */
    .screen-bars {
      display: flex;
      gap: 6px;
      justify-content: center;
      margin-top: 24px;
    }
    .screen-bar {
      width: 8px;
      border-radius: 4px;
      background: linear-gradient(180deg, var(--cyan), var(--purple));
      animation: barAnim 1.2s ease-in-out infinite;
    }
    .screen-bar:nth-child(1) { height: 24px; animation-delay: 0s; }
    .screen-bar:nth-child(2) { height: 40px; animation-delay: .15s; }
    .screen-bar:nth-child(3) { height: 32px; animation-delay: .3s; }
    .screen-bar:nth-child(4) { height: 48px; animation-delay: .45s; }
    .screen-bar:nth-child(5) { height: 28px; animation-delay: .6s; }
    @keyframes barAnim {
      0%,100% { transform: scaleY(1); opacity:.8; }
      50% { transform: scaleY(.5); opacity:.4; }
    }

    /* ── PRICING ── */
    .pricing-section {
      max-width: 1100px;
      margin: 0 auto;
    }
    .pricing-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 20px;
      margin-top: 56px;
    }
    .price-card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 18px;
      padding: 32px 24px;
      text-align: center;
      transition: all .3s;
      position: relative;
      overflow: hidden;
      backdrop-filter: blur(12px);
    }
    .price-card.featured {
      border-color: var(--cyan);
      box-shadow: 0 0 50px rgba(0,212,255,0.15);
    }
    .price-card.featured::before {
      content: 'פופולרי';
      position: absolute;
      top: 16px;
      left: 50%;
      transform: translateX(-50%);
      background: linear-gradient(135deg, var(--cyan), var(--purple));
      color: #fff;
      font-size: 10px;
      letter-spacing: 2px;
      padding: 4px 14px;
      border-radius: 100px;
      font-family: 'Heebo', sans-serif;
      font-weight: 700;
    }
    .price-tier {
      font-family: 'Orbitron', monospace;
      font-size: 13px;
      letter-spacing: 2px;
      color: var(--muted);
      margin-bottom: 16px;
    }
    .price-val {
      font-family: 'Orbitron', monospace;
      font-size: 36px;
      font-weight: 900;
      color: #fff;
      line-height: 1;
      margin-bottom: 4px;
    }
    .price-val sup { font-size: 16px; vertical-align: super; }
    .price-val .period { font-size: 13px; color: var(--muted); font-weight: 400; }
    .price-sep {
      height: 1px;
      background: var(--border);
      margin: 24px 0;
    }
    .price-feats {
      list-style: none;
      text-align: right;
      display: flex;
      flex-direction: column;
      gap: 10px;
      margin-bottom: 28px;
    }
    .price-feats li {
      font-size: 13px;
      color: var(--muted);
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .price-feats li::before {
      content: '✓';
      color: var(--cyan);
      font-weight: 700;
      flex-shrink: 0;
    }
    .price-card .btn { width: 100%; text-align: center; display: block; }

    /* ── HOW IT WORKS ── */
    .how-section {
      max-width: 1100px;
      margin: 0 auto;
    }
    .steps-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 0;
      margin-top: 56px;
      position: relative;
    }
    .steps-grid::before {
      content: '';
      position: absolute;
      top: 36px;
      left: 12%;
      right: 12%;
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--cyan), var(--purple), var(--magenta), transparent);
    }
    .step {
      text-align: center;
      padding: 0 20px;
    }
    .step-num {
      width: 72px; height: 72px;
      border-radius: 50%;
      background: var(--dark2);
      border: 2px solid var(--cyan);
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 20px;
      font-family: 'Orbitron', monospace;
      font-size: 22px;
      font-weight: 900;
      color: var(--cyan);
      box-shadow: 0 0 20px rgba(0,212,255,0.3);
      position: relative;
      z-index: 1;
    }
    .step-title {
      font-family: 'Orbitron', monospace;
      font-size: 12px;
      font-weight: 700;
      color: #fff;
      margin-bottom: 10px;
    }
    .step-desc { font-size: 13px; color: var(--muted); line-height: 1.6; }

    /* ── RANKS ── */
    .ranks-section {
      max-width: 1100px;
      margin: 0 auto;
    }
    .ranks-row {
      display: flex;
      gap: 16px;
      margin-top: 48px;
      justify-content: center;
      flex-wrap: wrap;
    }
    .rank-badge {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px;
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 24px 20px;
      min-width: 120px;
      transition: all .25s;
    }
    .rank-badge:hover {
      transform: translateY(-6px);
      box-shadow: 0 16px 40px rgba(0,0,0,0.4);
    }
    .rank-icon { font-size: 36px; }
    .rank-name {
      font-family: 'Orbitron', monospace;
      font-size: 11px;
      font-weight: 700;
      color: #fff;
      letter-spacing: 1px;
    }
    .rank-xp { font-size: 11px; color: var(--muted); }
    .rank-badge.active { border-color: var(--cyan); box-shadow: 0 0 20px rgba(0,212,255,0.2); }

    /* ── LOGIN MODAL ── */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.85);
      backdrop-filter: blur(12px);
      z-index: 200;
      display: flex;
      align-items: center;
      justify-content: center;
      opacity: 0;
      pointer-events: none;
      transition: opacity .3s;
    }
    .modal-overlay.open {
      opacity: 1;
      pointer-events: all;
    }
    .modal {
      background: var(--dark3);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 48px 40px;
      width: 420px;
      max-width: 95vw;
      position: relative;
      box-shadow: 0 40px 100px rgba(0,0,0,0.6), 0 0 60px rgba(0,212,255,0.08);
      transform: translateY(20px);
      transition: transform .3s;
    }
    .modal-overlay.open .modal { transform: translateY(0); }
    .modal-close {
      position: absolute;
      top: 20px; left: 20px;
      background: none;
      border: none;
      color: var(--muted);
      font-size: 20px;
      cursor: pointer;
      transition: color .2s;
    }
    .modal-close:hover { color: #fff; }
    .modal-logo {
      text-align: center;
      margin-bottom: 32px;
    }
    .modal-logo span {
      font-family: 'Orbitron', monospace;
      font-size: 20px;
      font-weight: 900;
      letter-spacing: 3px;
    }
    .modal-tabs {
      display: flex;
      background: rgba(255,255,255,0.04);
      border-radius: 8px;
      padding: 4px;
      margin-bottom: 28px;
    }
    .modal-tab {
      flex: 1;
      padding: 9px;
      text-align: center;
      font-size: 13px;
      font-weight: 700;
      color: var(--muted);
      cursor: pointer;
      border-radius: 6px;
      transition: all .2s;
    }
    .modal-tab.active {
      background: linear-gradient(135deg, rgba(0,212,255,0.15), rgba(168,85,247,0.15));
      color: #fff;
    }
    .google-btn {
      width: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      padding: 13px;
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 8px;
      color: #fff;
      font-size: 14px;
      cursor: pointer;
      transition: all .2s;
      margin-bottom: 20px;
    }
    .google-btn:hover {
      background: rgba(255,255,255,0.1);
      border-color: rgba(255,255,255,0.2);
    }
    .or-divider {
      display: flex;
      align-items: center;
      gap: 12px;
      color: var(--muted);
      font-size: 12px;
      margin-bottom: 20px;
    }
    .or-divider::before, .or-divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--border);
    }
    .form-field {
      margin-bottom: 14px;
    }
    .form-field label {
      display: block;
      font-size: 11px;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      color: var(--muted);
      margin-bottom: 6px;
    }
    .form-field input {
      width: 100%;
      background: rgba(255,255,255,0.04);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 12px 14px;
      color: #fff;
      font-size: 14px;
      font-family: 'Heebo', sans-serif;
      outline: none;
      transition: border-color .2s;
    }
    .form-field input:focus {
      border-color: var(--cyan);
      box-shadow: 0 0 0 3px rgba(0,212,255,0.1);
    }
    .modal .btn-primary {
      width: 100%;
      padding: 14px;
      font-size: 12px;
      margin-top: 6px;
      display: block;
      text-align: center;
      border: none;
    }

    /* OTP screen */
    .otp-screen { display: none; }
    .otp-screen.show { display: block; }
    .otp-info {
      text-align: center;
      margin-bottom: 24px;
    }
    .otp-info p { font-size: 14px; color: var(--muted); margin-top: 8px; }
    .otp-inputs {
      display: flex;
      gap: 10px;
      justify-content: center;
      margin-bottom: 24px;
      direction: ltr;
    }
    .otp-inputs input {
      width: 46px; height: 56px;
      background: rgba(0,0,0,0.3);
      border: 1px solid var(--border);
      border-radius: 8px;
      text-align: center;
      font-family: 'Orbitron', monospace;
      font-size: 22px;
      font-weight: 700;
      color: var(--cyan);
      outline: none;
      transition: border-color .2s;
    }
    .otp-inputs input:focus {
      border-color: var(--cyan);
      box-shadow: 0 0 0 3px rgba(0,212,255,0.15);
    }

    /* ── FOOTER ── */
    footer {
      position: relative;
      z-index: 1;
      border-top: 1px solid var(--border);
      padding: 56px 48px 40px;
      background: var(--dark2);
    }
    .footer-inner {
      max-width: 1100px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1.5fr 1fr 1fr 1fr;
      gap: 48px;
      margin-bottom: 48px;
    }
    .footer-brand p {
      font-size: 14px;
      color: var(--muted);
      line-height: 1.7;
      margin: 12px 0 20px;
    }
    .footer-col h5 {
      font-family: 'Orbitron', monospace;
      font-size: 11px;
      letter-spacing: 2px;
      color: var(--cyan);
      margin-bottom: 16px;
    }
    .footer-col ul { list-style: none; display: flex; flex-direction: column; gap: 10px; }
    .footer-col a {
      font-size: 14px;
      color: var(--muted);
      text-decoration: none;
      transition: color .2s;
    }
    .footer-col a:hover { color: #fff; }
    .footer-bottom {
      max-width: 1100px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-top: 28px;
      border-top: 1px solid rgba(255,255,255,0.05);
    }
    .footer-bottom p { font-size: 12px; color: var(--muted); }
    .footer-socials { display: flex; gap: 12px; }
    .social-btn {
      width: 36px; height: 36px;
      background: rgba(255,255,255,0.04);
      border: 1px solid var(--border);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 16px;
      cursor: pointer;
      transition: all .2s;
      text-decoration: none;
      color: var(--muted);
    }
    .social-btn:hover { border-color: var(--cyan); color: var(--cyan); }

    /* ── ANIMATIONS ── */
    @keyframes fadeInDown {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* ── SCROLL REVEAL ── */
    .reveal {
      opacity: 0;
      transform: translateY(30px);
      transition: opacity .6s ease, transform .6s ease;
    }
    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }

    /* ── RESPONSIVE ── */
    @media (max-width: 900px) {
      nav { padding: 16px 24px; }
      .nav-links { display: none; }
      section { padding: 72px 24px; }
      .modes-inner { grid-template-columns: 1fr; }
      .pricing-grid { grid-template-columns: 1fr 1fr; }
      .steps-grid { grid-template-columns: 1fr 1fr; gap: 24px; }
      .steps-grid::before { display: none; }
      .footer-inner { grid-template-columns: 1fr 1fr; }
    }
    @media (max-width: 600px) {
      .hero-title { font-size: 36px; }
      .hero-stats { gap: 24px; }
      .pricing-grid { grid-template-columns: 1fr; }
      .steps-grid { grid-template-columns: 1fr; }
      .footer-inner { grid-template-columns: 1fr; }
      .footer-bottom { flex-direction: column; gap: 16px; }
    }

    /* glitch effect for brand */
    .glitch {
      position: relative;
    }
    .glitch::before, .glitch::after {
      content: attr(data-text);
      position: absolute;
      inset: 0;
      font-family: 'Orbitron', monospace;
    }
    .glitch::before {
      color: var(--cyan);
      clip-path: polygon(0 30%, 100% 30%, 100% 50%, 0 50%);
      transform: translateX(-2px);
      animation: glitch1 4s infinite;
    }
    .glitch::after {
      color: var(--magenta);
      clip-path: polygon(0 60%, 100% 60%, 100% 75%, 0 75%);
      transform: translateX(2px);
      animation: glitch2 4s infinite;
    }
    @keyframes glitch1 {
      0%,95%,100% { opacity:0; }
      96% { opacity:1; transform: translateX(-4px); }
      98% { opacity:1; transform: translateX(4px); }
    }
    @keyframes glitch2 {
      0%,95%,100% { opacity:0; }
      97% { opacity:1; transform: translateX(4px); }
      99% { opacity:1; transform: translateX(-4px); }
    }
  </style>
</head>
<body>

<!-- ════════════════ NAV ════════════════ -->
<nav>
  <a href="#" class="nav-logo">
    <span class="c">ESCAPE</span><span class="m">IQ</span>
  </a>
  <ul class="nav-links">
    <li><a href="#features">פיצ'רים</a></li>
    <li><a href="#modes">מודים</a></li>
    <li><a href="#pricing">מחירים</a></li>
    <li><a href="#ranks">דירוגים</a></li>
  </ul>
  <div class="nav-cta">
    <button class="btn btn-ghost" onclick="openModal()">כניסה</button>
    <button class="btn btn-primary" onclick="openModal()">התחל בחינם</button>
  </div>
</nav>

<!-- ════════════════ HERO ════════════════ -->
<section class="hero">
  <div class="hero-blob blob1"></div>
  <div class="hero-blob blob2"></div>
  <div class="hero-blob blob3"></div>

  <div>
    <div class="hero-badge">
      <span class="badge-dot"></span>
      פלטפורמת חדרי הבריחה החכמה
    </div>

    <h1 class="hero-title">
      <span class="line1">צור. שחק.</span>
      <span class="line2 glitch" data-text="ESCAPE.">ESCAPE.</span>
    </h1>

    <p class="hero-sub">
      בנה חדרי בריחה ושאלונים אינטראקטיביים ברמה הבאה.
      עם מודים מתקדמים, דירוגים, ואפקטים שמוחקים גבולות.
    </p>

    <div class="hero-actions">
      <button class="btn btn-primary btn-hero" onclick="openModal()">▶ התחל בחינם</button>
      <a href="#features" class="btn btn-ghost btn-hero">גלה עוד ↓</a>
    </div>

    <div class="hero-stats">
      <div class="hero-stat">
        <span class="stat-val">10K+</span>
        <span class="stat-label">משתמשים</span>
      </div>
      <div class="hero-stat">
        <span class="stat-val">50K+</span>
        <span class="stat-label">חידונים</span>
      </div>
      <div class="hero-stat">
        <span class="stat-val">99.9%</span>
        <span class="stat-label">זמינות</span>
      </div>
      <div class="hero-stat">
        <span class="stat-val">4.9★</span>
        <span class="stat-label">דירוג</span>
      </div>
    </div>

    <div class="hero-cards-row">
      <div class="room-card">
        <div class="room-icon">💻</div>
        <div class="room-name">CYBER BREACH</div>
        <div class="room-desc">מסע בעולם פריצת סייבר עם אפקטי גליץ'</div>
        <span class="room-tag">LIVE</span>
      </div>
      <div class="room-card">
        <div class="room-icon">🔬</div>
        <div class="room-name">LAB ESCAPE</div>
        <div class="room-desc">מעבדה מסתורית עם ניסויים כימיים</div>
        <span class="room-tag">HOT</span>
      </div>
      <div class="room-card">
        <div class="room-icon">⚔️</div>
        <div class="room-name">DUNGEON RUN</div>
        <div class="room-desc">מבצר עתיק עם חידות ומכשולים</div>
        <span class="room-tag">NEW</span>
      </div>
      <div class="room-card">
        <div class="room-icon">🚔</div>
        <div class="room-name">PRISON BREAK</div>
        <div class="room-desc">בריחה מהכלא – כל שנייה חשובה</div>
        <span class="room-tag">EPIC</span>
      </div>
    </div>
  </div>
</section>

<!-- ════════════════ FEATURES ════════════════ -->
<section id="features">
  <div class="features">
    <div class="reveal">
      <span class="section-tag">פיצ'רים</span>
      <h2 class="section-title">הכל במקום אחד.<br/><span>בלי פשרות.</span></h2>
      <p class="section-sub">מערכת שלמה ליצירה, שיתוף ומשחק – עם טכנולוגיה מהמוח של EscapeIQ.</p>
    </div>

    <div class="features-grid">
      <div class="feat-card reveal">
        <span class="feat-icon">🗺️</span>
        <div class="feat-title">מפת חדר בריחה</div>
        <p class="feat-desc">בנה מפה ויזואלית עם חדרים, אייקונים, ונתיבים. כל שאלה נכונה פותחת דלת לחדר הבא.</p>
        <span class="feat-chip chip-free">חינמי</span>
      </div>
      <div class="feat-card reveal">
        <span class="feat-icon">🎮</span>
        <div class="feat-title">מודים מתקדמים</div>
        <p class="feat-desc">Combo × 2, חנות מטבעות, כדורים נופלים, הימורים – כל משחק שונה לחלוטין.</p>
        <span class="feat-chip chip-pro">PRO</span>
      </div>
      <div class="feat-card reveal">
        <span class="feat-icon">🤖</span>
        <div class="feat-title">יצירה עם AI</div>
        <p class="feat-desc">תן ל-AI ליצור שאלות, רמזים ותסריטים שלמים בשבריר שנייה.</p>
        <span class="feat-chip chip-ultra">ULTRA</span>
      </div>
      <div class="feat-card reveal">
        <span class="feat-icon">📊</span>
        <div class="feat-title">סטטיסטיקות חיות</div>
        <p class="feat-desc">עקוב אחרי XP, רצפים יומיים, דירוג, ותוצאות של כל שאלון בזמן אמת.</p>
        <span class="feat-chip chip-free">חינמי</span>
      </div>
      <div class="feat-card reveal">
        <span class="feat-icon">🌐</span>
        <div class="feat-title">שיתוף בלי גבולות</div>
        <p class="feat-desc">קישור ציבורי, QR, הטמעה באתר חיצוני, ואפילו דומיין מותאם אישית.</p>
        <span class="feat-chip chip-ultra">ULTRA</span>
      </div>
      <div class="feat-card reveal">
        <span class="feat-icon">🎨</span>
        <div class="feat-title">עיצוב מותאם אישית</div>
        <p class="feat-desc">בחר theme – Cyber, Lab, Prison – ועצב כל פיקסל לפי הסגנון שלך.</p>
        <span class="feat-chip chip-pro">PRO</span>
      </div>
    </div>
  </div>
</section>

<!-- ════════════════ MODES ════════════════ -->
<section id="modes" class="modes-section">
  <div class="modes-inner">
    <div>
      <div class="reveal">
        <span class="section-tag">מודי משחק</span>
        <h2 class="section-title">משחק שונה<br/><span>בכל פעם.</span></h2>
        <p class="section-sub">שלב מודים שונים ליצירת חוויה ייחודית לכל שאלון.</p>
      </div>
      <div class="modes-list reveal">
        <div class="mode-item active">
          <span class="mode-emoji">⚡</span>
          <div class="mode-info">
            <h4>מוד על זמן</h4>
            <p>ענה מהר יותר – קבל יותר נקודות. הטיימר לוחץ!</p>
          </div>
        </div>
        <div class="mode-item">
          <span class="mode-emoji">🔥</span>
          <div class="mode-info">
            <h4>מוד קומבו</h4>
            <p>רצף נכון = כפל נקודות עד X2. טעות אחת ומאפס!</p>
          </div>
        </div>
        <div class="mode-item">
          <span class="mode-emoji">🎲</span>
          <div class="mode-info">
            <h4>מוד הימורים</h4>
            <p>צדקת? בחר מ-3 תיבות לפרס. טעית? עונשים אקראיים!</p>
          </div>
        </div>
        <div class="mode-item">
          <span class="mode-emoji">🏪</span>
          <div class="mode-info">
            <h4>מוד חנות</h4>
            <p>אסוף מטבעות, קנה רמזים, הקפאות ותחבולות נגד יריבים.</p>
          </div>
        </div>
      </div>
    </div>
    <div class="modes-display reveal">
      <div class="modes-screen">
        <div class="screen-glow"></div>
        <div class="screen-content">
          <span class="screen-icon">🏆</span>
          <div class="screen-label">COMBO × 2.4</div>
          <div class="screen-bars">
            <div class="screen-bar"></div>
            <div class="screen-bar"></div>
            <div class="screen-bar"></div>
            <div class="screen-bar"></div>
            <div class="screen-bar"></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ════════════════ HOW IT WORKS ════════════════ -->
<section>
  <div class="how-section">
    <div class="reveal" style="text-align:center">
      <span class="section-tag">איך זה עובד</span>
      <h2 class="section-title">פשוט <span>להפליא.</span></h2>
    </div>
    <div class="steps-grid reveal">
      <div class="step">
        <div class="step-num">01</div>
        <div class="step-title">נרשם</div>
        <p class="step-desc">צור חשבון עם Google או אימייל תוך שניות. ללא כרטיס אשראי.</p>
      </div>
      <div class="step">
        <div class="step-num">02</div>
        <div class="step-title">צור</div>
        <p class="step-desc">בחר theme, הוסף שאלות ומיין מודים. AI יעזור לך בכל שלב.</p>
      </div>
      <div class="step">
        <div class="step-num">03</div>
        <div class="step-title">שתף</div>
        <p class="step-desc">שלח קישור, QR, או הטמע באתר שלך. המשחק מתחיל.</p>
      </div>
      <div class="step">
        <div class="step-num">04</div>
        <div class="step-title">עלה בדרגה</div>
        <p class="step-desc">אסוף XP, עלה בליגה, וקבל תארים מיוחדים כמו Master Mind.</p>
      </div>
    </div>
  </div>
</section>

<!-- ════════════════ PRICING ════════════════ -->
<section id="pricing">
  <div class="pricing-section">
    <div class="reveal" style="text-align:center">
      <span class="section-tag">מחירים</span>
      <h2 class="section-title">תוכנית <span>לכל אחד.</span></h2>
      <p class="section-sub" style="margin:0 auto">התחל בחינם. שדרג כשאתה מוכן.</p>
    </div>

    <div class="pricing-grid reveal">
      <!-- FREE -->
      <div class="price-card">
        <div class="price-tier">NOOB</div>
        <div class="price-val">0<span class="period"> / חודש</span></div>
        <div class="price-sep"></div>
        <ul class="price-feats">
          <li>5 פרויקטים</li>
          <li>עד 15 משתתפים</li>
          <li>15 שאלות לשאלון</li>
          <li>קישור ציבורי</li>
          <li>תמיכת קהילה</li>
        </ul>
        <button class="btn btn-ghost" onclick="openModal()">התחל עכשיו</button>
      </div>

      <!-- PRO -->
      <div class="price-card featured">
        <div class="price-tier" style="margin-top:20px">PRO</div>
        <div class="price-val"><sup>$</sup>7.99<span class="period"> / חודש</span></div>
        <div class="price-sep"></div>
        <ul class="price-feats">
          <li>10 פרויקטים</li>
          <li>40 משתתפים</li>
          <li>25 שאלות לשאלון</li>
          <li>QR + אופציות עיצוב</li>
          <li>עד 5 אפקטים קוליים</li>
          <li>AI ל-2 פרויקטים</li>
        </ul>
        <button class="btn btn-primary" onclick="openModal()">בחר PRO</button>
      </div>

      <!-- ULTRA -->
      <div class="price-card">
        <div class="price-tier">ULTRA</div>
        <div class="price-val"><sup>$</sup>13.99<span class="period"> / חודש</span></div>
        <div class="price-sep"></div>
        <ul class="price-feats">
          <li>שאלונים ללא הגבלה</li>
          <li>משתתפים ללא הגבלה</li>
          <li>שאלות ללא הגבלה</li>
          <li>הטמעה באתר חיצוני</li>
          <li>עד 15 אפקטים קוליים</li>
          <li>AI ל-5 פרויקטים</li>
        </ul>
        <button class="btn btn-ghost" onclick="openModal()">בחר ULTRA</button>
      </div>

      <!-- BUSINESS -->
      <div class="price-card">
        <div class="price-tier">BUSINESS</div>
        <div class="price-val"><sup>$</sup>17.99<span class="period"> / חודש</span></div>
        <div class="price-sep"></div>
        <ul class="price-feats">
          <li>הכל ב-ULTRA</li>
          <li>דומיין מותאם אישית</li>
          <li>50 אפקטים קוליים</li>
          <li>AI ללא הגבלה</li>
          <li>תמיכה עדיפות גבוהה</li>
          <li>לוגו מותאם אישית</li>
        </ul>
        <button class="btn btn-ghost" onclick="openModal()">בחר BUSINESS</button>
      </div>
    </div>
  </div>
</section>

<!-- ════════════════ RANKS ════════════════ -->
<section id="ranks" style="background:linear-gradient(180deg,transparent,rgba(168,85,247,0.04),transparent)">
  <div class="ranks-section">
    <div class="reveal" style="text-align:center">
      <span class="section-tag">מערכת דירוגים</span>
      <h2 class="section-title">אסוף XP. <span>עלה בדרגה.</span></h2>
      <p class="section-sub" style="margin:0 auto 0">השג תארים מיוחדים. הפוך ל-Legend.</p>
    </div>
    <div class="ranks-row reveal">
      <div class="rank-badge">
        <span class="rank-icon">🐣</span>
        <span class="rank-name">NOOB</span>
        <span class="rank-xp">0 XP</span>
      </div>
      <div class="rank-badge">
        <span class="rank-icon">⚔️</span>
        <span class="rank-name">WARRIOR</span>
        <span class="rank-xp">500 XP</span>
      </div>
      <div class="rank-badge active">
        <span class="rank-icon">🧠</span>
        <span class="rank-name">HACKER</span>
        <span class="rank-xp">2,000 XP</span>
      </div>
      <div class="rank-badge">
        <span class="rank-icon">💀</span>
        <span class="rank-name">MASTER MIND</span>
        <span class="rank-xp">5,000 XP</span>
      </div>
      <div class="rank-badge">
        <span class="rank-icon">👁️</span>
        <span class="rank-name">SHADOW</span>
        <span class="rank-xp">10,000 XP</span>
      </div>
      <div class="rank-badge">
        <span class="rank-icon">⚡</span>
        <span class="rank-name">GOD MODE</span>
        <span class="rank-xp">25,000 XP</span>
      </div>
    </div>
  </div>
</section>

<!-- ════════════════ FOOTER ════════════════ -->
<footer>
  <div class="footer-inner">
    <div class="footer-brand">
      <div class="nav-logo" style="font-size:18px;display:inline-block">
        <span class="c">ESCAPE</span><span class="m">IQ</span>
      </div>
      <p>פלטפורמת חדרי הבריחה החכמה. צור, שחק ועלה בדרגה – בלי גבולות.</p>
      <div style="display:flex;gap:10px">
        <a href="#" class="social-btn">𝕏</a>
        <a href="#" class="social-btn">📸</a>
        <a href="#" class="social-btn">💼</a>
      </div>
    </div>
    <div class="footer-col">
      <h5>מוצר</h5>
      <ul>
        <li><a href="#">פיצ'רים</a></li>
        <li><a href="#">מודים</a></li>
        <li><a href="#">מחירים</a></li>
        <li><a href="#">דירוגים</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h5>חברה</h5>
      <ul>
        <li><a href="#">אודות</a></li>
        <li><a href="#">בלוג</a></li>
        <li><a href="#">קהילה</a></li>
        <li><a href="#">שותפים</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h5>תמיכה</h5>
      <ul>
        <li><a href="#">מרכז עזרה</a></li>
        <li><a href="#">פרטיות</a></li>
        <li><a href="#">תנאי שימוש</a></li>
        <li><a href="#">צור קשר</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <p>© 2025 EscapeIQ. כל הזכויות שמורות.</p>
    <div class="footer-socials">
      <span style="font-size:12px;color:var(--muted)">עוצב באהבה ☕</span>
    </div>
  </div>
</footer>

<!-- ════════════════ LOGIN MODAL ════════════════ -->
<div class="modal-overlay" id="modalOverlay" onclick="handleOverlayClick(event)">
  <div class="modal">
    <button class="modal-close" onclick="closeModal()">✕</button>

    <div class="modal-logo">
      <span><span style="color:var(--cyan)">ESCAPE</span><span style="color:var(--magenta)">IQ</span></span>
    </div>

    <!-- LOGIN/REGISTER FORM -->
    <div id="authForm">
      <div class="modal-tabs">
        <div class="modal-tab active" onclick="switchTab(this,'login')">כניסה</div>
        <div class="modal-tab" onclick="switchTab(this,'register')">הרשמה</div>
      </div>

      <button class="google-btn" onclick="handleGoogle()">
        <svg width="18" height="18" viewBox="0 0 48 48"><path fill="#EA4335" d="M24 9.5c3.54 0 6.71 1.22 9.21 3.6l6.85-6.85C35.9 2.38 30.47 0 24 0 14.62 0 6.51 5.38 2.56 13.22l7.98 6.19C12.43 13.72 17.74 9.5 24 9.5z"/><path fill="#4285F4" d="M46.98 24.55c0-1.57-.15-3.09-.38-4.55H24v9.02h12.94c-.58 2.96-2.26 5.48-4.78 7.18l7.73 6c4.51-4.18 7.09-10.36 7.09-17.65z"/><path fill="#FBBC05" d="M10.53 28.59c-.48-1.45-.76-2.99-.76-4.59s.27-3.14.76-4.59l-7.98-6.19C.92 16.46 0 20.12 0 24c0 3.88.92 7.54 2.56 10.78l7.97-6.19z"/><path fill="#34A853" d="M24 48c6.48 0 11.93-2.13 15.89-5.81l-7.73-6c-2.15 1.45-4.92 2.3-8.16 2.3-6.26 0-11.57-4.22-13.47-9.91l-7.98 6.19C6.51 42.62 14.62 48 24 48z"/></svg>
        המשך עם Google
      </button>

      <div class="or-divider">או</div>

      <div class="form-field">
        <label>אימייל</label>
        <input type="email" id="emailInput" placeholder="you@example.com"/>
      </div>
      <div class="form-field" id="passwordField">
        <label>סיסמה</label>
        <input type="password" id="passInput" placeholder="••••••••"/>
      </div>
      <div class="form-field" id="nameField" style="display:none">
        <label>שם משתמש</label>
        <input type="text" id="nameInput" placeholder="GamerPro123"/>
      </div>

      <button class="btn btn-primary" onclick="handleAuth()">כניסה ←</button>
    </div>

    <!-- OTP SCREEN -->
    <div id="otpForm" class="otp-screen">
      <div class="otp-info">
        <h3 style="font-family:'Orbitron',monospace;font-size:16px;color:#fff">אימות זהות</h3>
        <p>שלחנו קוד 6 ספרות לאימייל שלך</p>
      </div>
      <div class="otp-inputs">
        <input type="text" maxlength="1" class="otp-in" onInput="otpNav(this,0)"/>
        <input type="text" maxlength="1" class="otp-in" onInput="otpNav(this,1)"/>
        <input type="text" maxlength="1" class="otp-in" onInput="otpNav(this,2)"/>
        <input type="text" maxlength="1" class="otp-in" onInput="otpNav(this,3)"/>
        <input type="text" maxlength="1" class="otp-in" onInput="otpNav(this,4)"/>
        <input type="text" maxlength="1" class="otp-in" onInput="otpNav(this,5)"/>
      </div>
      <button class="btn btn-primary" onclick="verifyOtp()" style="width:100%;padding:14px;border:none">אמת קוד ←</button>
      <p style="text-align:center;font-size:12px;color:var(--muted);margin-top:14px">לא קיבלת? <a href="#" style="color:var(--cyan)" onclick="resendOtp(event)">שלח שוב</a></p>
    </div>
  </div>
</div>

<script>
  // ── SCROLL REVEAL ──
  const revealEls = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (e.isIntersecting) { e.target.classList.add('visible'); }
    });
  }, { threshold: 0.12 });
  revealEls.forEach(el => observer.observe(el));

  // ── MODE TABS ──
  document.querySelectorAll('.mode-item').forEach(item => {
    item.addEventListener('click', () => {
      document.querySelectorAll('.mode-item').forEach(i => i.classList.remove('active'));
      item.classList.add('active');
    });
  });

  // ── MODAL ──
  function openModal() {
    document.getElementById('modalOverlay').classList.add('open');
  }
  function closeModal() {
    document.getElementById('modalOverlay').classList.remove('open');
  }
  function handleOverlayClick(e) {
    if (e.target === document.getElementById('modalOverlay')) closeModal();
  }

  let currentTab = 'login';
  function switchTab(el, tab) {
    document.querySelectorAll('.modal-tab').forEach(t => t.classList.remove('active'));
    el.classList.add('active');
    currentTab = tab;
    const nameField = document.getElementById('nameField');
    const btn = document.querySelector('#authForm .btn-primary');
    if (tab === 'register') {
      nameField.style.display = 'block';
      btn.textContent = 'הרשם ←';
    } else {
      nameField.style.display = 'none';
      btn.textContent = 'כניסה ←';
    }
  }

  function handleGoogle() {
    alert('כניסה עם Google תהיה זמינה בקרוב!');
  }

  function handleAuth() {
    const email = document.getElementById('emailInput').value;
    if (!email || !email.includes('@')) {
      alert('נא להזין כתובת אימייל תקינה');
      return;
    }
    // Show OTP screen
    document.getElementById('authForm').style.display = 'none';
    document.getElementById('otpForm').classList.add('show');
    document.querySelectorAll('.otp-in')[0].focus();
  }

  function otpNav(el, idx) {
    const inputs = document.querySelectorAll('.otp-in');
    if (el.value.length === 1 && idx < 5) {
      inputs[idx + 1].focus();
    }
  }

  function verifyOtp() {
    const code = Array.from(document.querySelectorAll('.otp-in')).map(i => i.value).join('');
    if (code.length < 6) { alert('נא להזין את כל 6 הספרות'); return; }
    alert('✅ אימות הצליח! ברוך הבא ל-EscapeIQ');
    closeModal();
  }

  function resendOtp(e) {
    e.preventDefault();
    alert('קוד חדש נשלח לאימייל שלך!');
  }

  // keyboard close
  document.addEventListener('keydown', e => { if (e.key === 'Escape') closeModal(); });
</script>
</body>
</html>
